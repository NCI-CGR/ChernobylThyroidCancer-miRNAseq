#!/bin/bash

mkdir log

module load snakemake fastqc cutadapt multiqc STAR R|| exit 1

sbcmd="sbatch --cpus-per-task={threads} --mem=128g --output=log/snakejob_%j.out --time=150:00:00"
snakemake -pr --cluster "$sbcmd" --keep-going --jobs 100 --latency-wait 120 all
